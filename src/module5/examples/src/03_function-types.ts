/** Normal functions */
function addToNum(a: number, b: number): number {
    return a + b
}

/** Async functions */
async function addToNumAsync(a: number, b: number): Promise<number> {
    return a + b
}

export {};